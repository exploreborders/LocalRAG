# Database Configuration
# For local development (outside Docker):
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=rag_system
# POSTGRES_USER=christianhein  # Default macOS user
# POSTGRES_PASSWORD=  # No password for local

# For Docker usage (when running app inside containers) - CURRENTLY ACTIVE:
POSTGRES_HOST=localhost
POSTGRES_USER=christianhein
POSTGRES_PASSWORD=

# Elasticsearch Configuration (using instead of OpenSearch)
# For local development - CURRENTLY ACTIVE:
OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200
OPENSEARCH_USER=elastic
OPENSEARCH_PASSWORD=changeme

# For Docker usage (when running app inside containers):
# OPENSEARCH_HOST=elasticsearch

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Cache Configuration
CACHE_ENABLED=true
CACHE_TYPE=redis
CACHE_TTL_HOURS=24

# Other settings
OLLAMA_BASE_URL=http://localhost:11434